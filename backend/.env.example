# Leave GROQ_API_KEY empty to use the built-in mock provider (fully playable offline).
GROQ_API_KEY=
GROQ_MODEL=llama-3.3-70b-versatile
LLM_PROVIDER=auto            # auto | groq | mock
DATA_DIR=./data
CORS_ORIGINS=http://localhost:3000
